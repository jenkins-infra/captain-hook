apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "captain-hook.fullname" . }}-test-connection"
  labels:
    {{- include "captain-hook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "captain-hook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
